# Infrastructure

Infrastructure-as-code organization. Never commit credentials or state files.

- `modules/` — reusable, documented infrastructure building blocks.
- `environments/` — isolated environment composition and configuration.
- `platform/` — shared foundation services, policy, networking, and observability.

