# Docker

Container-related assets. Keep builds reproducible, minimal, non-root where practical, and free of secrets.

- `development/` — local development container and orchestration support.
- `production/` — hardened production image and runtime definitions.

